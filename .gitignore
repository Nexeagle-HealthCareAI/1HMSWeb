# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright e2e (real login state, screenshots/traces of a real environment — never commit)
e2e/.auth
e2e/report
test-results
playwright-report
.env.e2e
.playwright-mcp

# Claude Code project-scoped MCP config — machine-specific absolute paths (npm global install
# location), personal dev-tool setup, not shared team config.
.mcp.json
